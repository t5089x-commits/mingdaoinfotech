/* ===========================================================
   共用「教師專區」密鑰 —— 四科通用同一把。
   之前每科各自的 assets/progress.js 也有這個 teacherKey（單元鎖定用），
   兩邊要保持同一個值。要換密鑰時，這裡跟四科的 assets/progress.js
   都要一起改。
   =========================================================== */
window.COURSE_PROGRESS = {
  teacherKey: "t5089x"
};
