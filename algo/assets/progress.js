/* ===========================================================
   課程進度設定 —— 老師每週只要改這裡！
   ===========================================================
   unlockedUpTo : 「已經開放到第幾單元」（只算主線 01~18，附錄單元不受影響）。
                  0  = 全部鎖住
                  1  = 只開放單元 1
                  18 = 全部開放（本課程共 18 個主線單元）
                  每週上完一個單元，把這個數字 +1 即可。

   目前這份是統整後的「基本版」草稿，先把 unlockedUpTo 設為 18（全部開放），
   方便老師自己整份瀏覽、調整內容。之後正式上課要鎖單元進度時，
   把下面的數字改小即可；要預覽全部單元，在網址後加上
   ?key=你的密鑰（例如 index.html?key=t5089x）。

   ※ 兩個「附」補充單元（unit02b 函式補充、unit06b 堆疊vs佇列比較）
      不計入鎖定序號，永遠開放瀏覽。
   =========================================================== */
window.COURSE_PROGRESS = {
  unlockedUpTo: 18,
  teacherKey: "t5089x"
};
